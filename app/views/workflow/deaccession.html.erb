<h4>Deaccession Physical Objects</h4>
<p style="font-weight: 700">
  This will permanently remove a Physical Object from all workflow, views, and search unless specifically searching for
  Deaccessioned Physical Objects
</p>
<p class="warn">
  Make sure to notify ALF that the item has been deaccessioned.
</p>
<%= form_for @physical_object, url: {controller: 'workflow', action: 'deaccession_ajax_post'}, method: :post do |f| %>
  <table class="_50">
    <tr>
      <th>IU Barcode</th>
      <td><%= f.text_field :iu_barcode, {class: 'iu_barcode'} %></td>
    </tr>
    <tr>
      <th colspan="2">Comment</th>
    </tr>
    <tr>
      <td colspan="2">
        <%= text_area_tag :comment %>
      </td>
    </tr>
    <tr>
      <td colspan="2"><%= f.submit "Deaccession" %></td>
    </tr>
  </table>
<% end %>